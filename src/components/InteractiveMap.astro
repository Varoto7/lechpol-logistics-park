<div class="w-full max-w-7xl mx-auto py-12 px-4 relative">
    
    <div class="mb-8">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-white">Masterplan Parku Logistycznego</h2>
        <p class="text-gray-500 mt-2">Wybierz obiekt na mapie, aby zobaczyć szczegóły.</p>
        
        <!-- Legenda -->
        <div class="flex flex-wrap gap-4 mt-4 text-sm font-medium">
            <div class="flex items-center gap-2"><span class="w-4 h-4 rounded-full bg-emerald-500"></span> Dostępne</div>
            <div class="flex items-center gap-2"><span class="w-4 h-4 rounded-full bg-amber-500"></span> Rezerwacja</div>
            <div class="flex items-center gap-2"><span class="w-4 h-4 rounded-full bg-red-500"></span> Wynajęte</div>
        </div>
    </div>

    <!-- Główny Kontener Mapy z przewijaniem -->
    <div class="relative w-full rounded-2xl shadow-lg border border-gray-100 dark:border-slate-800 bg-slate-50 dark:bg-slate-900 overflow-hidden">
        <div id="map-scroll-container" class="w-full overflow-auto max-h-[75vh] cursor-grab active:cursor-grabbing select-none p-2">
            <div id="map-inner-wrapper" class="min-w-[1000px] w-full relative">
                
                <svg width="100%" height="auto" viewBox="0 0 1920 2210" fill="none" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" id="map-svg-element">
                    <rect x="0.5" y="0.5" width="1919" height="2209" fill="url(#pattern0_5_2)" stroke="#02FFE6"/>
                    
                    <!-- Ścieżki stref -->
                    <path id="hala-1" d="M854 1422L774.5 1656L1423 1883L1506.5 1641.5L854 1422Z" stroke="#3CF2FF" stroke-width="8"/>
                    <path id="hala-2" d="M1456 1174L1323 1130.5L1185 1529L1429.5 1611.5L1589 1152L1478 1115L1456 1174Z" stroke="#3CF2FF" stroke-width="8"/>
                    <path id="hala-3" d="M1040 1092.5L851 1188.5L766.5 1022.5L953.5 926L1040 1092.5Z" stroke="#3CF2FF" stroke-width="8"/>
                    <path id="hala-4" d="M971 705.5L680 855L766 1022L953 925L1039.5 1092.5L1140.5 1040.5L971 705.5Z" stroke="#3CF2FF" stroke-width="8"/>
                    
                    <defs>
                        <pattern id="pattern0_5_2" patternContentUnits="objectBoundingBox" width="1" height="1">
                            <use xlink:href="#image0_5_2" transform="scale(0.000520833 0.000452489)"/>
                        </pattern>
                        <image id="image0_5_2" width="1920" height="2210" preserveAspectRatio="none" href="/mapa-tlo.jpg" />
                    </defs>
                </svg>

            </div>
        </div>
    </div>

    <!-- Dymek (Tooltip) podążający za kursorem -->
    <div id="map-tooltip" class="fixed hidden pointer-events-none bg-slate-900 text-white p-3 rounded-xl shadow-xl z-50 text-sm transition-opacity duration-150 opacity-0">
        <div id="tooltip-title" class="font-bold text-base mb-1">Nazwa Hali</div>
        <div id="tooltip-area" class="text-emerald-400 font-medium">Powierzchnia</div>
        <div id="tooltip-status" class="text-gray-300 text-xs mt-1 uppercase tracking-wider">Status</div>
    </div>

    <!-- Modal ze szczegółami po kliknięciu -->
    <div id="map-modal" class="fixed inset-0 z-[100] hidden items-center justify-center bg-slate-900/50 backdrop-blur-sm opacity-0 transition-opacity duration-300">
        <div class="bg-white dark:bg-slate-800 w-full max-w-lg rounded-2xl p-6 m-4 shadow-2xl transform scale-95 transition-transform duration-300" id="modal-content">
            <div class="flex justify-between items-start mb-4">
                <div>
                    <h3 id="modal-title" class="text-2xl font-bold text-slate-900 dark:text-white">Nazwa Hali</h3>
                    <span id="modal-status-badge" class="inline-block mt-2 px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wide">Status</span>
                </div>
                <button onclick="closeModal()" class="text-gray-400 hover:text-red-500 transition-colors">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                </button>
            </div>
            
            <div class="space-y-4 text-slate-600 dark:text-gray-300">
                <div class="flex justify-between py-2 border-b border-gray-100 dark:border-slate-700">
                    <span class="font-medium">Powierzchnia:</span>
                    <span id="modal-area" class="font-bold text-slate-900 dark:text-white">0 m²</span>
                </div>
                <div>
                    <p class="font-medium mb-1">Opis szczegółowy:</p>
                    <p id="modal-desc" class="text-sm leading-relaxed whitespace-pre-line">Brak opisu.</p>
                </div>
            </div>
            
            <a href="/kontakt" class="block w-full text-center mt-8 bg-sky-500 hover:bg-sky-600 text-white font-bold py-3 rounded-xl transition-colors">
                Zapytaj o tę strefę
            </a>
            <button onclick="closeModal()" class="w-full mt-3 bg-slate-100 hover:bg-slate-200 dark:bg-slate-700 dark:hover:bg-slate-600 text-slate-900 dark:text-white font-medium py-3 rounded-xl transition-colors">
                Zamknij okno
            </button>
        </div>
    </div>

</div>

<script is:inline>
    const statusConfig = {
        'dostepna': { color: '#10b981', opacity: '0.65', hoverOpacity: '0.9', badgeClass: 'bg-emerald-100 text-emerald-700' },
        'rezerwacja': { color: '#f59e0b', opacity: '0.65', hoverOpacity: '0.9', badgeClass: 'bg-amber-100 text-amber-700' },
        'wynajeta': { color: '#ef4444', opacity: '0.65', hoverOpacity: '0.9', badgeClass: 'bg-red-100 text-red-700' }
    };

    let tooltipTimeout = null;

    document.addEventListener('DOMContentLoaded', async () => {
        const tooltip = document.getElementById('map-tooltip');
        const scrollContainer = document.getElementById('map-scroll-container');
        
        // --- Ustawienie widoku lekko niżej środka zdjęcia ---
        setTimeout(() => {
            if (scrollContainer) {
                scrollContainer.scrollLeft = (scrollContainer.scrollWidth - scrollContainer.clientWidth) / 2;
                scrollContainer.scrollTop = (scrollContainer.scrollHeight - scrollContainer.clientHeight) * 0.58;
            }
        }, 50);

        // --- POBIERANIE DANYCH Z BAZY D1 ---
        try {
            const response = await fetch('/api/get-zones');
            window.mapData = await response.json();

            if (!window.mapData || window.mapData.error) return;
        } catch(e) {
            console.error("Błąd połączenia z mapą:", e);
            return; 
        }
        
        Object.keys(window.mapData).forEach(id => {
            const data = window.mapData[id];
            if (!data || typeof data !== 'object') return;

            let svgElement = document.getElementById(id) || document.getElementById(id.replace('hala', 'hala-'));
            
            if (svgElement) {
                const currentStatus = data.status || 'dostepna';
                const config = statusConfig[currentStatus] || statusConfig['dostepna'];

                svgElement.setAttribute('fill', config.color);
                svgElement.setAttribute('fill-opacity', config.opacity);
                
                svgElement.style.cursor = 'pointer';
                svgElement.style.pointerEvents = 'visiblePainted';
                svgElement.style.stroke = '#ffffff';
                svgElement.style.strokeWidth = '3px';
                svgElement.style.transition = 'fill-opacity 0.15s ease-in-out';

                // Hover
                svgElement.addEventListener('mouseenter', () => {
                    if (tooltipTimeout) {
                        clearTimeout(tooltipTimeout);
                        tooltipTimeout = null;
                    }

                    if (svgElement.nextElementSibling) {
                        svgElement.parentElement.appendChild(svgElement);
                    }

                    svgElement.setAttribute('fill-opacity', config.hoverOpacity);
                    
                    document.getElementById('tooltip-title').innerText = data.nazwa || 'Strefa';
                    document.getElementById('tooltip-area').innerText = data.powierzchnia || '-';
                    document.getElementById('tooltip-status').innerText = currentStatus;
                    
                    tooltip.classList.remove('hidden');
                    requestAnimationFrame(() => {
                        tooltip.classList.remove('opacity-0');
                    });
                });

                // Ruch myszy
                svgElement.addEventListener('mousemove', (e) => {
                    tooltip.style.left = (e.clientX + 15) + 'px';
                    tooltip.style.top = (e.clientY + 15) + 'px';
                });

                // Zjazd myszy
                svgElement.addEventListener('mouseleave', () => {
                    svgElement.setAttribute('fill-opacity', config.opacity);
                    
                    tooltip.classList.add('opacity-0');
                    tooltipTimeout = setTimeout(() => {
                        tooltip.classList.add('hidden');
                    }, 150);
                });

                // Kliknięcie
                svgElement.addEventListener('click', (e) => {
                    e.stopPropagation();
                    openModal(data);
                });
            }
        });
    });

    function openModal(data) {
        const modal = document.getElementById('map-modal');
        const modalContent = document.getElementById('modal-content');
        
        document.getElementById('modal-title').innerText = data.nazwa || 'Strefa';
        document.getElementById('modal-area').innerText = data.powierzchnia || '-';
        document.getElementById('modal-desc').innerText = data.opis || 'Brak opisu.';
        
        const badge = document.getElementById('modal-status-badge');
        const currentStatus = data.status || 'dostepna';
        const config = statusConfig[currentStatus] || statusConfig['dostepna'];

        badge.innerText = currentStatus;
        badge.className = `inline-block mt-2 px-3 py-1 text-xs font-bold rounded-full uppercase tracking-wide ${config.badgeClass}`;
        
        modal.classList.remove('hidden');
        modal.classList.add('flex');
        
        requestAnimationFrame(() => {
            modal.classList.remove('opacity-0');
            modalContent.classList.remove('scale-95');
            modalContent.classList.add('scale-100');
        });
    }

    window.closeModal = function() {
        const modal = document.getElementById('map-modal');
        const modalContent = document.getElementById('modal-content');
        
        modal.classList.add('opacity-0');
        modalContent.classList.remove('scale-100');
        modalContent.classList.add('scale-95');
        
        setTimeout(() => {
            modal.classList.add('hidden');
            modal.classList.remove('flex');
        }, 300);
    }

    document.getElementById('map-modal-wirus').addEventListener('click', (e) => {
        if(e.target === document.getElementById('map-modal')) {
            closeModal();
        }
    });
</script>